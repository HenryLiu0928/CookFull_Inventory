-- NOT IN USE

-- CREATE DATABASE cookfull;

-- CREATE TABLE inventory (
--     inventory_id SERIAL PRIMARY KEY,
--     description VARCHAR(255) UNIQUE,
--     consumed BOOLEAN,
--     quantity INTEGER,
--     unit VARCHAR(20)
-- );
